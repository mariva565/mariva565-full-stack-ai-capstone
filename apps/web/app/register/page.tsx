import { ScreenPlaceholder } from "@/components/screen-placeholder";

export default function RegisterPage() {
  return (
    <ScreenPlaceholder
      title="Register"
      description="Placeholder screen for user registration flow (JWT auth)."
    />
  );
}
