import { ScreenPlaceholder } from "@/components/screen-placeholder";

export default function LoginPage() {
  return (
    <ScreenPlaceholder title="Login" description="Placeholder screen for user sign-in flow." />
  );
}
